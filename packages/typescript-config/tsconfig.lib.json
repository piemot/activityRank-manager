{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // Library monorepo type emissions
    "declaration": true,
    "composite": true,
    "sourceMap": true,
    "declarationMap": true,

    // Assumes a bundler *isn't* used for libs
    "module": "NodeNext",
    "outDir": "dist",
  }
}
